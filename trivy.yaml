# Trivy configuration for expense-ui CI
# Exit code 0 so Trivy findings don't block the pipeline.
# Use severity: CRITICAL if you want to block on critical findings only.
exit-code: 0
severity: HIGH,CRITICAL
